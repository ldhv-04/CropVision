/**
 * @deprecated Frozen StationShell compatibility entry. Canonical Station uses
 * SoilzeProShell + <Slot />; do not add consumers or move/delete this export
 * without a separately approved plan.
 */
export { StationShell, default } from './StationShell';
