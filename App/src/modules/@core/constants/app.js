/**
 * App-wide constants — Layer 1 Core
 *
 * [M1] Single source of truth for values shared across modules.
 *       Previously FIXED_ADMIN_EMAIL was hardcoded in 2+ frontend files.
 */

/** The email address of the fixed admin account (cannot be deleted or demoted). */
export const FIXED_ADMIN_EMAIL = 'admin@cropvision.local';
