/**
 * Legacy theme alias — re-exports from the canonical theme module.
 * Use '@core/constants/theme' directly in new code.
 */
export {
  COLORS,
  DARK_COLORS,
  LIGHT_COLORS,
  SPACING,
  RADIUS,
  FONT_SIZE,
  SHADOWS,
} from '../modules/@core/constants/theme';