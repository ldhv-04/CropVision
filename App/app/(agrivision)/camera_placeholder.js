/**
 * (agrivision)/camera_placeholder.js — Tab 2: Quét bệnh lá cây (LeafScanTab)
 */

import React from 'react';
import { LeafScanTab } from '../../src/modules/agrivision/screens/LeafScanTab';

export default function CameraScanScreen() {
  return <LeafScanTab />;
}
