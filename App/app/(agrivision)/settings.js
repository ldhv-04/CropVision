/**
 * Route: /(agrivision)/settings — profile and settings tab.
 * Thin composition root; implementation lives in the Agrivision module.
 */
export { SettingsScreen as default } from '../../src/modules/agrivision';
